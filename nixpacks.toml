[phases.setup]
nixPkgs = ["nodejs_20"]

# Explicitly disable Caddy — Express serves everything (API + static files)
[phases.caddy]
cmds = []

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.cjs"
